version: '3.7'

services:
  makepdfs:
    container_name: googledrive
    build:
      context: .
      dockerfile: Dockerfile
    image: "docker.pkg.github.com/nikhilkumar13/actions-googledrive/googledrive:${TAG:-latest}"
